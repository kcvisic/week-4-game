let DEFAULT_VALUE = 0;

let generateRandomNumber = function(min, max) {
	console.log("Hello, I generate a random number");
}

var GemGame = {
    matchThisNumber: DEFAULT_VALUE,
    wins: DEFAULT_VALUE,
    losses: DEFAULT_VALUE,
    gems: [DEFAULT_VALUE, DEFAULT_VALUE, DEFAULT_VALUE, DEFAULT_VALUE],
    score: DEFAULT_VALUE,
    generateRandomNumber: generateRandomNumber
}

